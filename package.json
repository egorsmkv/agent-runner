{
  "name": "agent-runner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "agent": "node index.mjs",
    "build": "node --check index.mjs && node --check server/*.mjs && node --check actions/*/index.mjs && node --check commands/*/index.mjs && node --check utils/*.mjs",
    "start": "node server/index.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
